// -------------------------------------------------------------------------
// 1. MODAL COCINA GENERAL
// -------------------------------------------------------------------------
function KitchenProductionModal({ onClose, activeProduction, setActiveProduction, recipesDB, setProductionHistory }: any) {
    const [view, setView] = useState<'category' | 'product' | 'recipe'>(activeProduction ? 'recipe' : 'category');
    const [selectedCategory, setSelectedCategory] = useState("");
    const [selectedProduct, setSelectedProduct] = useState<any>(null);
    const [targetUnits, setTargetUnits] = useState(0); 
    const [currentTime, setCurrentTime] = useState(Date.now());
    const [checkedIngredients, setCheckedIngredients] = useState<Set<number>>(new Set());

    const groupedRecipes = recipesDB.reduce((acc:any, recipe:Recipe) => {
        if (!acc[recipe.category]) acc[recipe.category] = [];
        acc[recipe.category].push(recipe);
        return acc;
    }, {});

    useEffect(() => {
        if (activeProduction && !selectedProduct) {
            const found = recipesDB.find((r: Recipe) => r.name === activeProduction.recipeName);
            if (found) {
                setSelectedProduct(found);
                setTargetUnits(activeProduction.targetUnits);
                setCheckedIngredients(new Set(found.ingredients.map((_:any, i:number) => i)));
            }
        }
        const timer = setInterval(() => setCurrentTime(Date.now()), 1000);
        return () => clearInterval(timer);
    }, [activeProduction]);

    const handleProductSelect = (product: any) => { setSelectedProduct(product); setTargetUnits(product.baseYield); setView('recipe'); setCheckedIngredients(new Set()); };
    const toggleCheck = (idx: number) => { if(activeProduction) return; const next = new Set(checkedIngredients); if (next.has(idx)) next.delete(idx); else next.add(idx); setCheckedIngredients(next); };
    const allIngredientsChecked = selectedProduct && checkedIngredients.size === selectedProduct.ingredients.length;
    const canStart = targetUnits > 0 && allIngredientsChecked;
    const handleStart = () => { if (!canStart) return; setActiveProduction({ recipeName: selectedProduct.name, targetUnits: targetUnits, unit: selectedProduct.unit, startTime: Date.now(), status: 'running' }); };
    const handleFinish = () => {
        const endTime = Date.now();
        const durationSeconds = (endTime - activeProduction.startTime) / 1000;
        const newRecord = { id: Date.now(), date: new Date().toLocaleDateString(), timestamp: endTime, recipeName: activeProduction.recipeName, quantity: activeProduction.targetUnits, unit: activeProduction.unit, durationSeconds: durationSeconds, startTime: new Date(activeProduction.startTime).toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'}), endTime: new Date(endTime).toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'}) };
        setProductionHistory((prev: ProductionRecord[]) => [...prev, newRecord]);
        alert(`Producción Finalizada y Guardada.`);
        setActiveProduction(null);
        onClose();
    };
    const multiplier = selectedProduct && targetUnits > 0 ? targetUnits / selectedProduct.baseYield : 0;
    const elapsedTime = activeProduction ? currentTime - activeProduction.startTime : 0;
    const formatTimer = (ms: number) => { const totalSecs = Math.floor(ms / 1000); const hrs = Math.floor(totalSecs / 3600); const mins = Math.floor((totalSecs % 3600) / 60); const secs = totalSecs % 60; return `${hrs > 0 ? hrs + ':' : ''}${mins.toString().padStart(2,'0')}:${secs.toString().padStart(2,'0')}`; };

    return (
        <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-900/80 backdrop-blur-sm p-4 animate-in fade-in zoom-in-95 duration-200">
            <div className="bg-white rounded-2xl shadow-2xl w-full max-w-5xl h-[90vh] flex flex-col overflow-hidden">
                <div className="px-8 py-5 border-b border-slate-100 flex justify-between items-center bg-slate-50">
                    <div><h2 className="text-xl font-bold text-slate-800 flex items-center gap-2"><UtensilsCrossed className="text-amber-600" /> Cocina General</h2><p className="text-slate-500 text-sm">{activeProduction ? 'Producción en curso...' : (view === 'recipe' ? `Configurando: ${selectedProduct.name}` : 'Seleccione receta')}</p></div>
                    <button onClick={onClose} className="p-2 hover:bg-slate-200 rounded-full transition-colors"><X size={24} className="text-slate-500" /></button>
                </div>
                <div className="flex-1 overflow-y-auto bg-slate-50 p-8">
                    {view === 'category' && !activeProduction && (<div className="grid grid-cols-1 md:grid-cols-3 gap-6 h-full content-center"><button onClick={() => { setSelectedCategory('Panificados'); setView('product'); }} className="flex flex-col items-center justify-center p-10 bg-white border-2 border-amber-100 rounded-3xl hover:border-amber-400 hover:shadow-xl transition-all group"><Wheat size={48} className="text-amber-600 mb-4 group-hover:scale-110 transition-transform"/> <span className="text-2xl font-bold text-slate-800">Panificados</span></button><button onClick={() => { setSelectedCategory('Salsas'); setView('product'); }} className="flex flex-col items-center justify-center p-10 bg-white border-2 border-red-100 rounded-3xl hover:border-red-400 hover:shadow-xl transition-all group"><Droplet size={48} className="text-red-600 mb-4 group-hover:scale-110 transition-transform"/> <span className="text-2xl font-bold text-slate-800">Salsas</span></button><button onClick={() => { setSelectedCategory('Prep'); setView('product'); }} className="flex flex-col items-center justify-center p-10 bg-white border-2 border-blue-100 rounded-3xl hover:border-blue-400 hover:shadow-xl transition-all group"><Clock size={48} className="text-blue-600 mb-4 group-hover:scale-110 transition-transform"/> <span className="text-2xl font-bold text-slate-800">Prep / Otros</span></button></div>)}
                    {view === 'product' && !activeProduction && (<div className="space-y-6"><button onClick={() => setView('category')} className="text-sm font-bold text-slate-400 hover:text-slate-600 flex items-center gap-1"><ArrowLeft size={16}/> Volver</button><div className="grid grid-cols-2 md:grid-cols-4 gap-4">{groupedRecipes[selectedCategory]?.map((prod: any) => (<button key={prod.id} onClick={() => handleProductSelect(prod)} className="bg-white p-6 rounded-2xl border border-slate-200 hover:border-amber-500 hover:shadow-md transition-all text-left group"><h3 className="font-bold text-lg text-slate-800 mb-1 group-hover:text-amber-600 transition-colors">{prod.name}</h3><p className="text-xs text-slate-500">Receta base: {prod.baseYield} {prod.unit}</p>{prod.warning && <span className="inline-block mt-2 px-2 py-1 bg-blue-100 text-blue-700 text-[10px] font-bold rounded">{prod.warning}</span>}</button>))}</div></div>)}
                    {view === 'recipe' && selectedProduct && (<div className="flex flex-col h-full">{!activeProduction && <button onClick={() => setView('product')} className="text-sm font-bold text-slate-400 hover:text-slate-600 flex items-center gap-1 mb-4"><ArrowLeft size={16}/> Volver</button>}<div className="flex flex-1 gap-8"><div className="w-1/3 bg-white p-6 rounded-2xl border border-slate-200 flex flex-col shadow-sm"><h3 className="font-bold text-slate-400 uppercase text-xs mb-6 tracking-wider">{activeProduction ? 'Estado Actual' : 'Planificación'}</h3><div className="mb-8"><label className="block text-sm font-bold text-slate-600 mb-2">Cantidad a Producir</label><div className="relative"><input type="number" value={targetUnits} disabled={!!activeProduction} onChange={(e) => setTargetUnits(Math.max(0, Number(e.target.value)))} className={`w-full p-4 border-2 rounded-xl text-4xl font-black text-center outline-none transition-all ${activeProduction ? 'bg-slate-100 border-slate-200 text-slate-500' : 'bg-slate-50 border-amber-200 text-amber-600 focus:border-amber-500 focus:bg-white'}`} /><span className="absolute right-4 top-6 text-sm font-bold text-slate-300 uppercase">{selectedProduct.unit}</span></div></div>{activeProduction && (<div className="mt-auto mb-6 bg-slate-900 rounded-2xl p-6 text-center shadow-inner border border-slate-700 relative overflow-hidden"><div className="absolute top-0 left-0 w-full h-1 bg-gradient-to-r from-green-500 to-blue-500 animate-pulse"></div><span className="text-slate-400 text-xs font-bold uppercase tracking-widest block mb-2">Tiempo Transcurrido</span><span className="text-5xl font-mono font-bold text-white tracking-widest">{formatTimer(elapsedTime)}</span></div>)}{selectedProduct.warning && (<div className="bg-blue-50 p-4 rounded-xl border border-blue-100 flex items-start gap-3 mt-auto"><AlertCircle className="text-blue-500 shrink-0" size={20}/><div><h4 className="font-bold text-blue-700 text-xs uppercase">Recordatorio</h4><p className="text-sm text-blue-600 leading-tight">{selectedProduct.warning}</p></div></div>)}</div><div className="w-2/3 flex flex-col"><div className="bg-white rounded-2xl border border-slate-200 flex-1 flex flex-col overflow-hidden shadow-sm"><div className="p-4 border-b border-slate-100 bg-slate-50 flex justify-between items-center"><h3 className="font-bold text-slate-700 flex items-center gap-2"><Calculator size={18}/> Checklist de Insumos</h3><span className="text-xs font-bold text-slate-400 uppercase">Para {targetUnits} {selectedProduct.unit}</span></div><div className="flex-1 overflow-y-auto p-0"><table className="w-full text-sm"><thead className="bg-slate-50 text-slate-400 text-xs uppercase font-bold"><tr><th className="py-3 pl-6 text-left w-12">Check</th><th className="py-3 text-left">Ingrediente</th><th className="py-3 pr-6 text-right">Cantidad</th></tr></thead><tbody className="divide-y divide-slate-100">{selectedProduct.ingredients.map((ing: any, idx: number) => { const isChecked = checkedIngredients.has(idx); return (<tr key={idx} className={`transition-colors cursor-pointer ${isChecked ? 'bg-green-50/50' : 'hover:bg-slate-50'}`} onClick={() => toggleCheck(idx)}><td className="py-4 pl-6">{isChecked ? <CheckSquare className="text-green-500" size={20}/> : <Square className="text-slate-300" size={20}/>}</td><td className={`py-4 font-medium ${isChecked ? 'text-slate-400 line-through' : 'text-slate-700'}`}>{ing.name}</td><td className={`py-4 pr-6 text-right font-bold text-lg ${isChecked ? 'text-slate-400' : 'text-slate-900'}`}>{(ing.qty * multiplier).toFixed(3)} <span className="text-sm font-medium text-slate-400">{ing.unit}</span></td></tr>); })}</tbody></table></div><div className="p-6 border-t border-slate-200 bg-slate-50">{!activeProduction ? (<button onClick={handleStart} disabled={!canStart} className={`w-full py-4 font-bold rounded-xl text-lg transition-all shadow-lg active:scale-95 flex items-center justify-center gap-2 ${canStart ? 'bg-green-600 text-white hover:bg-green-500' : 'bg-slate-200 text-slate-400 cursor-not-allowed'}`} > {allIngredientsChecked ? <><Play size={20} fill="currentColor"/> INICIAR PRODUCCIÓN (TIMER)</> : `FALTAN CHECKS (${selectedProduct.ingredients.length - checkedIngredients.size})`} </button>) : (<button onClick={handleFinish} className="w-full py-4 bg-slate-900 text-white font-bold rounded-xl text-lg hover:bg-slate-800 transition-all shadow-lg active:scale-95 flex items-center justify-center gap-2"><CheckCircle2 size={20}/> FINALIZAR Y GUARDAR</button>)}</div></div></div></div></div>)}
                </div>
            </div>
        </div>
    );
}

// -------------------------------------------------------------------------
// 2. MODAL PROVEEDORES (FULL CODE - RESTAURADO)
// -------------------------------------------------------------------------
function SuppliersModal({ onClose }: { onClose: () => void }) {
    const [suppliers, setSuppliers] = useState([
        { id: 1, name: "Frigorífico El Toro", categories: ["Carnicería", "Fiambres"], cuit: "30-11223344-5", phone: "11-4455-6677", email: "pedidos@eltoro.com", address: "Av. Corrientes 1234", days: ["Lun", "Jue"] },
        { id: 2, name: "La Huerta del Sol", categories: ["Verduras"], cuit: "20-99887766-1", phone: "11-9876-5432", email: "ventas@huertasol.com.ar", address: "Mercado Central Nave 4", days: ["Mar", "Vie"] },
        { id: 3, name: "Distribuidora Norte", categories: ["Insumos", "Bebidas", "Aderezos"], cuit: "33-55667788-9", phone: "11-5555-0000", email: "contacto@disnorte.com", address: "Ruta 8 Km 20", days: ["Mié"] },
    ]);
    const [selectedId, setSelectedId] = useState<number | null>(null);
    const [isEditing, setIsEditing] = useState(false);
    const [searchTerm, setSearchTerm] = useState("");
    const emptyForm = { name: "", categories: [] as string[], cuit: "", phone: "", email: "", address: "", days: [] as string[] };
    const [formData, setFormData] = useState(emptyForm);
    const categoriesList = ["Carnicería", "Panificados", "Insumos", "Verduras", "Fiambres", "Aderezos", "Bebidas"];
    const weekDays = ["Lun", "Mar", "Mié", "Jue", "Vie", "Sáb"];

    const handleSelect = (s: any) => { setSelectedId(s.id); setFormData(s); setIsEditing(false); };
    const handleNew = () => { setSelectedId(null); setFormData(emptyForm); setIsEditing(true); };
    const handleSave = () => {
        if (!formData.name) return alert("El nombre es obligatorio");
        if (selectedId) setSuppliers(suppliers.map(s => s.id === selectedId ? { ...formData, id: selectedId } : s));
        else setSuppliers([...suppliers, { ...formData, id: Date.now() }]);
        setIsEditing(false); setSelectedId(null);
    };
    const handleDelete = () => { if (confirm("¿Estás seguro de eliminar este proveedor?")) { setSuppliers(suppliers.filter(s => s.id !== selectedId)); setSelectedId(null); setIsEditing(false); } };
    const toggleCategory = (cat: string) => { if (formData.categories.includes(cat)) setFormData({ ...formData, categories: formData.categories.filter(c => c !== cat) }); else setFormData({ ...formData, categories: [...formData.categories, cat] }); };
    const toggleDay = (day: string) => { if (formData.days.includes(day)) setFormData({ ...formData, days: formData.days.filter(d => d !== day) }); else setFormData({ ...formData, days: [...formData.days, day] }); };
    const filteredSuppliers = suppliers.filter(s => s.name.toLowerCase().includes(searchTerm.toLowerCase()));

    return (
        <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-900/80 p-4 animate-in fade-in zoom-in-95 duration-200">
            <div className="bg-white rounded-2xl w-full max-w-6xl h-[85vh] flex flex-col shadow-2xl overflow-hidden">
                <div className="px-8 py-5 border-b border-slate-100 flex justify-between items-center bg-white">
                    <div><h2 className="text-xl font-bold flex gap-2 text-slate-800 items-center"><Truck className="text-blue-600"/> Gestión de Proveedores</h2><p className="text-xs text-slate-400">Base de datos de contacto y logística</p></div>
                    <button onClick={onClose} className="p-2 hover:bg-slate-100 rounded-full transition-colors"><X size={24} className="text-slate-400"/></button>
                </div>
                <div className="flex flex-1 overflow-hidden bg-slate-50">
                    <div className="w-1/3 border-r border-slate-200 flex flex-col bg-white">
                        <div className="p-4 border-b border-slate-100"><div className="relative mb-3"><Search className="absolute left-3 top-3 text-slate-400" size={18} /><input type="text" placeholder="Buscar proveedor..." className="w-full pl-10 pr-4 py-2 bg-slate-50 border border-slate-200 rounded-lg text-sm outline-none focus:ring-2 focus:ring-blue-500" value={searchTerm} onChange={e => setSearchTerm(e.target.value)}/></div><button onClick={handleNew} className="w-full py-2 flex items-center justify-center gap-2 text-white font-bold bg-blue-600 rounded-lg hover:bg-blue-700 transition-colors text-sm"><Plus size={16} /> AGREGAR NUEVO</button></div>
                        <div className="flex-1 overflow-y-auto p-2 space-y-1">{filteredSuppliers.map(s => (<div key={s.id} onClick={() => handleSelect(s)} className={`p-4 rounded-xl cursor-pointer transition-all border ${selectedId === s.id ? 'bg-blue-600 text-white border-blue-600 shadow-lg' : 'bg-white hover:bg-slate-50 border-slate-100'}`}><div className="flex justify-between items-start mb-2"><h3 className="font-bold text-sm">{s.name}</h3>{selectedId !== s.id && <ChevronRight size={16} className="text-slate-300"/>}</div><div className="flex flex-wrap gap-1">{s.categories.map(c => <span key={c} className={`text-[10px] px-2 py-0.5 rounded-full font-bold ${selectedId === s.id ? 'bg-blue-500 text-white' : 'bg-slate-100 text-slate-500'}`}>{c}</span>)}</div></div>))}</div>
                    </div>
                    <div className="w-2/3 p-8 overflow-y-auto">
                        {(isEditing || selectedId) ? (<div className="max-w-2xl mx-auto"><div className="flex justify-between items-center mb-6"><h3 className="text-2xl font-bold text-slate-800">{isEditing ? (selectedId ? "Editar Proveedor" : "Nuevo Proveedor") : formData.name}</h3>{!isEditing && (<div className="flex gap-2"><button onClick={() => setIsEditing(true)} className="px-4 py-2 bg-slate-100 text-slate-600 font-bold rounded-lg hover:bg-slate-200">Editar</button><button onClick={handleDelete} className="px-4 py-2 bg-red-50 text-red-600 font-bold rounded-lg hover:bg-red-100"><Trash2 size={18}/></button></div>)}</div><div className="space-y-6"><div className="bg-white p-6 rounded-2xl border border-slate-200 shadow-sm"><label className="block text-xs font-bold text-slate-400 uppercase mb-3">Categorías (Rubros)</label><div className="flex flex-wrap gap-2">{categoriesList.map(cat => (<button key={cat} onClick={() => isEditing && toggleCategory(cat)} disabled={!isEditing} className={`px-3 py-1.5 rounded-lg text-xs font-bold border transition-all ${formData.categories.includes(cat) ? 'bg-blue-100 text-blue-700 border-blue-200' : 'bg-white text-slate-400 border-slate-200'} ${isEditing ? 'cursor-pointer hover:border-blue-400' : 'cursor-default'}`}>{cat}</button>))}</div></div><div className="grid grid-cols-2 gap-6"><div><label className="block text-xs font-bold text-slate-400 uppercase mb-1">Nombre / Razón Social</label><input disabled={!isEditing} type="text" value={formData.name} onChange={e => setFormData({...formData, name: e.target.value})} className="w-full p-3 bg-white border border-slate-200 rounded-lg outline-none focus:border-blue-500 font-bold text-slate-800 disabled:bg-slate-50"/></div><div><label className="block text-xs font-bold text-slate-400 uppercase mb-1">CUIT</label><input disabled={!isEditing} type="text" value={formData.cuit} onChange={e => setFormData({...formData, cuit: e.target.value})} className="w-full p-3 bg-white border border-slate-200 rounded-lg outline-none focus:border-blue-500 font-mono text-slate-600 disabled:bg-slate-50"/></div></div><div className="bg-white p-6 rounded-2xl border border-slate-200 shadow-sm space-y-4"><h4 className="font-bold text-slate-700 text-sm flex items-center gap-2"><User size={16}/> Información de Contacto</h4><div className="grid grid-cols-2 gap-4"><div><label className="block text-xs font-bold text-slate-400 uppercase mb-1">Teléfono / Celular</label><div className="flex gap-2"><input disabled={!isEditing} type="text" value={formData.phone} onChange={e => setFormData({...formData, phone: e.target.value})} className="w-full p-3 bg-white border border-slate-200 rounded-lg outline-none focus:border-blue-500"/>{!isEditing && formData.phone && (<button onClick={() => window.open(`https://wa.me/${formData.phone.replace(/[^0-9]/g, '')}`, '_blank')} className="p-3 bg-green-500 text-white rounded-lg hover:bg-green-600" title="Abrir WhatsApp"><Phone size={18}/></button>)}</div></div><div><label className="block text-xs font-bold text-slate-400 uppercase mb-1">Email</label><input disabled={!isEditing} type="email" value={formData.email} onChange={e => setFormData({...formData, email: e.target.value})} className="w-full p-3 bg-white border border-slate-200 rounded-lg outline-none focus:border-blue-500"/></div><div className="col-span-2"><label className="block text-xs font-bold text-slate-400 uppercase mb-1">Dirección Física</label><input disabled={!isEditing} type="text" value={formData.address} onChange={e => setFormData({...formData, address: e.target.value})} className="w-full p-3 bg-white border border-slate-200 rounded-lg outline-none focus:border-blue-500"/></div></div></div><div className="bg-white p-6 rounded-2xl border border-slate-200 shadow-sm"><h4 className="font-bold text-slate-700 text-sm flex items-center gap-2 mb-3"><CalendarIcon size={16}/> Días de Entrega / Visita</h4><div className="flex gap-2 justify-between">{weekDays.map(day => (<button key={day} disabled={!isEditing} onClick={() => toggleDay(day)} className={`w-12 h-12 rounded-full flex items-center justify-center font-bold text-xs transition-all border ${formData.days.includes(day) ? 'bg-blue-600 text-white border-blue-600' : 'bg-slate-50 text-slate-400 border-slate-200'}`}>{day}</button>))}</div></div>{isEditing && (<div className="flex gap-4 pt-4 border-t border-slate-200"><button onClick={() => { setIsEditing(false); if(!selectedId) setFormData(emptyForm); }} className="flex-1 py-3 text-slate-500 font-bold hover:bg-slate-100 rounded-xl">Cancelar</button><button onClick={handleSave} className="flex-1 py-3 bg-slate-900 text-white font-bold rounded-xl hover:bg-blue-600 shadow-lg">GUARDAR PROVEEDOR</button></div>)}</div></div>) : (<div className="h-full flex flex-col items-center justify-center text-slate-300"><Truck size={64} className="mb-4 opacity-50"/><p className="font-bold text-lg">Selecciona un proveedor para ver detalles</p><p className="text-sm">o crea uno nuevo desde el panel izquierdo.</p></div>)}
                    </div>
                </div>
            </div>
        </div>
    );
}

// -------------------------------------------------------------------------
// 3. MODAL GESTOR RECETAS (ABM COMPLETO)
// -------------------------------------------------------------------------
function RecipeManagerModal({ onClose, recipes, setRecipes }: any) {
    const [selectedRecipe, setSelectedRecipe] = useState<Recipe | null>(null);
    const [searchTerm, setSearchTerm] = useState("");
    const [isEditing, setIsEditing] = useState(false);
    const emptyRecipe: Recipe = { id: '', name: '', category: 'Panificados', baseYield: 0, unit: 'u', ingredients: [] };
    const [formData, setFormData] = useState<Recipe>(emptyRecipe);
    const categories = ['Panificados', 'Salsas', 'Prep'];

    const handleSelect = (r: Recipe) => { setSelectedRecipe(r); setFormData(JSON.parse(JSON.stringify(r))); setIsEditing(false); };
    const handleNew = () => { setSelectedRecipe(null); setFormData({ ...emptyRecipe, id: Date.now().toString() }); setIsEditing(true); };
    const handleSave = () => {
        if (!formData.name || formData.baseYield <= 0) return alert("Nombre y Rendimiento son obligatorios");
        if (selectedRecipe) setRecipes(recipes.map((r: Recipe) => r.id === formData.id ? formData : r));
        else setRecipes([...recipes, { ...formData, id: Date.now().toString() }]);
        setIsEditing(false); setSelectedRecipe(formData);
    };
    const handleDelete = () => { if (confirm("¿Borrar receta? Esto no se puede deshacer.")) { setRecipes(recipes.filter((r: Recipe) => r.id !== formData.id)); setSelectedRecipe(null); setIsEditing(false); } };
    const addIngredient = () => { setFormData({ ...formData, ingredients: [...formData.ingredients, { name: '', qty: 0, unit: 'kg' }] }); };
    const updateIngredient = (index: number, field: keyof Ingredient, value: any) => { const newIngredients = [...formData.ingredients]; newIngredients[index] = { ...newIngredients[index], [field]: value }; setFormData({ ...formData, ingredients: newIngredients }); };
    const removeIngredient = (index: number) => { const newIngredients = formData.ingredients.filter((_, i) => i !== index); setFormData({ ...formData, ingredients: newIngredients }); };
    const filteredRecipes = recipes.filter((r: Recipe) => r.name.toLowerCase().includes(searchTerm.toLowerCase()));

    return (
        <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-900/80 p-4 animate-in fade-in zoom-in-95 duration-200">
            <div className="bg-white rounded-2xl w-full max-w-6xl h-[85vh] flex flex-col shadow-2xl overflow-hidden">
                <div className="px-8 py-5 border-b border-slate-100 flex justify-between items-center bg-white"><div><h2 className="text-xl font-bold flex gap-2 text-slate-800 items-center"><BookOpen className="text-amber-600"/> Recetario Maestro</h2><p className="text-xs text-slate-400">Administrar fórmulas y rendimientos</p></div><button onClick={onClose} className="p-2 hover:bg-slate-100 rounded-full transition-colors"><X size={24} className="text-slate-400"/></button></div>
                <div className="flex flex-1 overflow-hidden bg-slate-50">
                    <div className="w-1/3 border-r border-slate-200 flex flex-col bg-white">
                        <div className="p-4 border-b border-slate-100"><div className="relative mb-3"><Search className="absolute left-3 top-3 text-slate-400" size={18} /><input type="text" placeholder="Buscar receta..." className="w-full pl-10 pr-4 py-2 bg-slate-50 border border-slate-200 rounded-lg text-sm outline-none focus:ring-2 focus:ring-amber-500" value={searchTerm} onChange={e => setSearchTerm(e.target.value)}/></div><button onClick={handleNew} className="w-full py-2 flex items-center justify-center gap-2 text-white font-bold bg-amber-600 rounded-lg hover:bg-amber-700 transition-colors text-sm"><Plus size={16} /> NUEVA RECETA</button></div>
                        <div className="flex-1 overflow-y-auto p-2 space-y-1">{filteredRecipes.map((r: Recipe) => (<div key={r.id} onClick={() => handleSelect(r)} className={`p-3 rounded-lg cursor-pointer transition-all border ${selectedRecipe?.id === r.id ? 'bg-amber-50 border-amber-500 shadow-sm' : 'bg-white hover:bg-slate-50 border-transparent'}`}><div className="flex justify-between items-center"><h3 className={`font-bold text-sm ${selectedRecipe?.id === r.id ? 'text-amber-900' : 'text-slate-700'}`}>{r.name}</h3><span className="text-[10px] uppercase font-bold text-slate-400 border px-1 rounded">{r.category}</span></div><p className="text-xs text-slate-400 mt-1">Rinde: {r.baseYield} {r.unit}</p></div>))}</div>
                    </div>
                    <div className="w-2/3 flex flex-col overflow-hidden">{isEditing || selectedRecipe ? (<div className="flex-1 overflow-y-auto p-8"><div className="max-w-3xl mx-auto"><div className="flex justify-between items-start mb-6"><h3 className="text-2xl font-bold text-slate-800 mb-1">{isEditing ? (selectedRecipe ? "Editar Receta" : "Nueva Receta") : formData.name}</h3>{!isEditing && (<div className="flex gap-2"><button onClick={() => setIsEditing(true)} className="px-4 py-2 bg-slate-100 text-slate-600 font-bold rounded-lg hover:bg-slate-200 flex items-center gap-2"><Edit size={16}/> Editar</button><button onClick={handleDelete} className="px-4 py-2 bg-red-50 text-red-600 font-bold rounded-lg hover:bg-red-100"><Trash2 size={16}/></button></div>)}</div><div className={`space-y-6 ${!isEditing ? 'pointer-events-none opacity-90' : ''}`}><div className="bg-white p-6 rounded-xl border border-slate-200 shadow-sm"><div className="grid grid-cols-2 gap-4"><div><label className="block text-xs font-bold text-slate-400 uppercase mb-1">Nombre</label><input type="text" value={formData.name} onChange={e => setFormData({...formData, name: e.target.value})} className="w-full p-2 border rounded-lg font-bold text-slate-800"/></div><div><label className="block text-xs font-bold text-slate-400 uppercase mb-1">Categoría</label><select value={formData.category} onChange={e => setFormData({...formData, category: e.target.value})} className="w-full p-2 border rounded-lg bg-white">{categories.map(c => <option key={c} value={c}>{c}</option>)}</select></div></div></div><div className="bg-white p-6 rounded-xl border border-slate-200 shadow-sm"><div className="flex justify-between items-center mb-4"><h4 className="font-bold text-slate-700 text-sm flex items-center gap-2"><List size={16}/> Ingredientes</h4>{isEditing && <button onClick={addIngredient} className="text-blue-600 text-xs font-bold flex items-center gap-1 hover:bg-blue-50 px-2 py-1 rounded"><Plus size={14}/> Agregar</button>}</div><table className="w-full text-sm"><thead className="bg-slate-50 text-slate-400 text-xs uppercase text-left"><tr><th className="p-2 pl-3">Insumo</th><th className="p-2 w-24">Cant.</th><th className="p-2 w-24">Unidad</th>{isEditing && <th className="p-2 w-10"></th>}</tr></thead><tbody className="divide-y divide-slate-100">{formData.ingredients.map((ing, idx) => (<tr key={idx}><td className="p-2"><input type="text" value={ing.name} onChange={e => updateIngredient(idx, 'name', e.target.value)} className="w-full p-2 border rounded font-medium" placeholder="Ej: Harina"/></td><td className="p-2"><input type="number" value={ing.qty} onChange={e => updateIngredient(idx, 'qty', parseFloat(e.target.value))} className="w-full p-2 border rounded text-center"/></td><td className="p-2"><select value={ing.unit} onChange={e => updateIngredient(idx, 'unit', e.target.value)} className="w-full p-2 border rounded bg-white"><option value="kg">kg</option><option value="lt">lt</option><option value="u">u</option><option value="gr">gr</option></select></td>{isEditing && <td className="p-2 text-center"><button onClick={() => removeIngredient(idx)} className="text-slate-400 hover:text-red-500"><Trash2 size={16}/></button></td>}</tr>))}</tbody></table></div><div className="bg-white p-6 rounded-xl border-l-4 border-amber-400 shadow-sm flex items-center justify-between"><div><h4 className="font-bold text-amber-800 text-sm uppercase">Rendimiento (Output)</h4></div><div className="flex gap-2 items-center"><input type="number" value={formData.baseYield} onChange={e => setFormData({...formData, baseYield: parseFloat(e.target.value)})} className="w-32 p-3 border-2 border-amber-200 rounded-lg text-2xl font-black text-center text-amber-600 outline-none"/><select value={formData.unit} onChange={e => setFormData({...formData, unit: e.target.value})} className="p-3 border rounded-lg bg-white font-bold text-slate-600"><option value="u">Unidades</option><option value="kg">Kilos</option><option value="lt">Litros</option></select></div></div></div>{isEditing && (<div className="flex gap-4 mt-8 pb-8"><button onClick={() => { setIsEditing(false); if(selectedRecipe) setFormData(selectedRecipe); else setSelectedRecipe(null); }} className="flex-1 py-3 text-slate-500 font-bold bg-white border border-slate-300 rounded-xl hover:bg-slate-50">Cancelar</button><button onClick={handleSave} className="flex-1 py-3 bg-slate-900 text-white font-bold rounded-xl hover:bg-green-600 shadow-lg flex items-center justify-center gap-2">GUARDAR RECETA</button></div>)}</div></div>) : (<div className="h-full flex flex-col items-center justify-center text-slate-300"><BookOpen size={64} className="mb-4 opacity-50"/><p className="font-bold text-lg">Selecciona una receta</p></div>)}</div></div></div></div>);
}

// -------------------------------------------------------------------------
// 4. MODAL REPORTES (EXPORTAR CSV)
// -------------------------------------------------------------------------
function ReportsModal({ onClose, history }: { onClose: () => void, history: ProductionRecord[] }) {
    const exportToCSV = () => {
        if (history.length === 0) return alert("No hay datos para exportar.");
        const headers = ["Fecha", "Hora Inicio", "Hora Fin", "Receta", "Cantidad", "Unidad", "Duracion (min)"];
        const rows = history.map(r => [r.date, r.startTime, r.endTime, r.recipeName, r.quantity, r.unit, (r.durationSeconds / 60).toFixed(2)]);
        const csvContent = "data:text/csv;charset=utf-8," + headers.join(",") + "\n" + rows.map(e => e.join(",")).join("\n");
        const encodedUri = encodeURI(csvContent);
        const link = document.createElement("a"); link.setAttribute("href", encodedUri); link.setAttribute("download", `reporte_cocina_${new Date().toISOString().slice(0,10)}.csv`); document.body.appendChild(link); link.click(); document.body.removeChild(link);
    };
    return (
        <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-900/80 p-4 animate-in fade-in zoom-in-95 duration-200">
            <div className="bg-white rounded-2xl w-full max-w-5xl h-[85vh] flex flex-col shadow-2xl overflow-hidden">
                <div className="px-8 py-5 border-b border-slate-100 flex justify-between items-center bg-white"><div><h2 className="text-xl font-bold flex gap-2 text-slate-800 items-center"><BarChart3 className="text-purple-600"/> Historial de Producción</h2></div><button onClick={onClose} className="p-2 hover:bg-slate-100 rounded-full transition-colors"><X size={24} className="text-slate-400"/></button></div>
                <div className="flex-1 overflow-y-auto bg-slate-50 p-8">
                    {history.length === 0 ? (<div className="h-full flex flex-col items-center justify-center text-slate-300"><Clock size={64} className="mb-4 opacity-50"/><p className="font-bold text-lg">No hay producciones registradas hoy</p></div>) : (<div className="bg-white rounded-xl shadow-sm border border-slate-200 overflow-hidden"><table className="w-full text-sm text-left"><thead className="bg-slate-50 text-slate-500 font-bold uppercase text-xs border-b border-slate-100"><tr><th className="p-4">Fecha</th><th className="p-4">Receta</th><th className="p-4">Producción</th><th className="p-4">Inicio / Fin</th><th className="p-4 text-right">Duración</th></tr></thead><tbody className="divide-y divide-slate-100">{history.slice().reverse().map((record) => (<tr key={record.id} className="hover:bg-slate-50 transition-colors"><td className="p-4 font-medium text-slate-600">{record.date}</td><td className="p-4 font-bold text-slate-800">{record.recipeName}</td><td className="p-4"><span className="bg-blue-100 text-blue-700 px-2 py-1 rounded text-xs font-bold">{record.quantity} {record.unit}</span></td><td className="p-4 text-xs text-slate-500">{record.startTime} - {record.endTime}</td><td className="p-4 text-right font-mono font-bold text-green-600">{(record.durationSeconds / 60).toFixed(1)} min</td></tr>))}</tbody></table></div>)}
                </div>
                <div className="p-6 border-t border-slate-100 bg-white flex justify-between items-center"><div className="text-xs text-slate-400">Total registros: <b>{history.length}</b></div><button onClick={exportToCSV} disabled={history.length === 0} className={`px-6 py-3 rounded-xl font-bold flex items-center gap-2 transition-all ${history.length === 0 ? 'bg-slate-100 text-slate-400 cursor-not-allowed' : 'bg-green-600 text-white hover:bg-green-700 shadow-lg'}`}><Download size={18}/> EXPORTAR A EXCEL</button></div>
            </div>
        </div>
    );
}

// -------------------------------------------------------------------------
// 5. MODAL STOCK ENTRY (COMPLETO CON COLORES)
// -------------------------------------------------------------------------
function StockEntryModal({ onClose }: { onClose: () => void }) {
    const [step, setStep] = useState(1); 
    const [category, setCategory] = useState("");
    const [isUnit, setIsUnit] = useState(false); 
    const [hasExpiration, setHasExpiration] = useState(false);
    const categories = [{ id: 'carniceria', label: 'Carnicería', icon: <Beef size={32}/>, color: 'bg-red-50 text-red-600 border-red-200 hover:bg-red-100' },{ id: 'panificados', label: 'Panificados', icon: <Wheat size={32}/>, color: 'bg-amber-50 text-amber-600 border-amber-200 hover:bg-amber-100' },{ id: 'insumos', label: 'Insumos', icon: <Soup size={32}/>, color: 'bg-blue-50 text-blue-600 border-blue-200 hover:bg-blue-100' },{ id: 'verduras', label: 'Verduras', icon: <Carrot size={32}/>, color: 'bg-green-50 text-green-600 border-green-200 hover:bg-green-100' },{ id: 'fiambres', label: 'Fiambres', icon: <Sandwich size={32}/>, color: 'bg-pink-50 text-pink-600 border-pink-200 hover:bg-pink-100' },{ id: 'aderezos', label: 'Aderezos', icon: <Droplet size={32}/>, color: 'bg-yellow-50 text-yellow-600 border-yellow-200 hover:bg-yellow-100' },{ id: 'bebidas', label: 'Bebidas', icon: <Wine size={32}/>, color: 'bg-purple-50 text-purple-600 border-purple-200 hover:bg-purple-100' }];
    return (
        <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-900/60 backdrop-blur-sm p-4 animate-in fade-in zoom-in-95 duration-200">
            <div className="bg-white rounded-2xl shadow-2xl w-full max-w-4xl max-h-[90vh] flex flex-col overflow-hidden">
                <div className="px-8 py-5 border-b border-slate-100 flex justify-between items-center bg-slate-50"><div><h2 className="text-xl font-bold text-slate-800 flex items-center gap-2"><Truck className="text-blue-600" /> Ingreso de Mercadería</h2><p className="text-slate-500 text-sm">{step === 1 ? 'Paso 1: Selecciona el rubro' : `Paso 2: Datos de ${category}`}</p></div><button onClick={onClose} className="p-2 hover:bg-slate-200 rounded-full transition-colors"><X size={24} className="text-slate-500" /></button></div>
                <div className="p-8 overflow-y-auto flex-1">
                    {step === 1 && (<div className="grid grid-cols-2 md:grid-cols-4 gap-4">{categories.map((cat) => (<button key={cat.id} onClick={() => { setCategory(cat.label); setStep(2); }} className={`flex flex-col items-center justify-center p-6 rounded-xl border-2 transition-all ${cat.color}`}><div className="mb-3">{cat.icon}</div><span className="font-bold text-sm uppercase">{cat.label}</span></button>))}</div>)}
                    {step === 2 && (<div className="grid grid-cols-1 lg:grid-cols-2 gap-8"><div className="space-y-6"><div><label className="block text-xs font-bold text-slate-500 uppercase mb-1">Producto</label><input type="text" placeholder="Ej: Coca Cola" className="w-full p-4 bg-slate-50 border border-slate-200 rounded-xl text-lg font-semibold outline-none focus:ring-2 focus:ring-blue-500" autoFocus /></div><div className="grid grid-cols-2 gap-4"><div><label className="block text-xs font-bold text-slate-500 uppercase mb-1">Cantidad</label><input type="number" className="w-full p-4 bg-slate-50 border border-slate-200 rounded-xl text-2xl font-bold text-slate-800 outline-none" /></div><div><label className="block text-xs font-bold text-slate-500 uppercase mb-1">Unidad</label><div className="flex bg-slate-100 p-1 rounded-xl"><button onClick={() => setIsUnit(false)} className={`flex-1 py-3 rounded-lg text-sm font-bold ${!isUnit ? 'bg-white shadow text-blue-600' : 'text-slate-400'}`}>KG</button><button onClick={() => setIsUnit(true)} className={`flex-1 py-3 rounded-lg text-sm font-bold ${isUnit ? 'bg-white shadow text-blue-600' : 'text-slate-400'}`}>UNID</button></div></div></div></div><div className="bg-slate-50 p-6 rounded-xl border border-slate-200 space-y-6"><h3 className="font-bold text-slate-700 flex items-center gap-2 text-sm uppercase"><CalendarIcon size={16}/> Trazabilidad</h3><div><label className="block text-xs font-bold text-slate-400 uppercase mb-1">Lote / Remito</label><input type="text" className="w-full p-3 bg-white border border-slate-200 rounded-lg font-mono" /></div><div className="p-4 bg-white rounded-lg border border-slate-200"><div className="flex items-center justify-between"><label className="font-bold text-slate-700 text-sm">¿Vencimiento?</label><input type="checkbox" checked={hasExpiration} onChange={(e) => setHasExpiration(e.target.checked)} className="w-5 h-5 accent-blue-600" /></div>{hasExpiration && <input type="date" className="w-full mt-3 p-3 bg-red-50 border border-red-100 text-red-800 rounded-lg font-bold" />}</div></div></div>)}
                </div>
                <div className="p-6 border-t border-slate-100 flex justify-end gap-3 bg-slate-50">{step === 2 && <button onClick={() => setStep(1)} className="px-6 py-3 rounded-xl border border-slate-300 font-bold hover:bg-white">Atrás</button>}{step === 2 && <button onClick={() => { alert("Guardado"); onClose(); setStep(1); }} className="px-8 py-3 rounded-xl bg-blue-600 text-white font-bold hover:bg-blue-700 flex items-center gap-2"><CheckCircle2 size={18}/> CONFIRMAR</button>}</div>
            </div>
        </div>
    );
}

// -------------------------------------------------------------------------
// 6. MODAL STOCK EXIT (COMPLETO)
// -------------------------------------------------------------------------
function StockExitModal({ onClose }: { onClose: () => void }) {
    const categories = [{ label: 'Carnicería', icon: <Beef size={30}/>, color: 'bg-red-100 text-red-700' }, { label: 'Panificados', icon: <Wheat size={30}/>, color: 'bg-amber-100 text-amber-700' }, { label: 'Insumos', icon: <Soup size={30}/>, color: 'bg-blue-100 text-blue-700' }, { label: 'Verduras', icon: <Carrot size={30}/>, color: 'bg-green-100 text-green-700' }, { label: 'Fiambres', icon: <Sandwich size={30}/>, color: 'bg-pink-100 text-pink-700' }, { label: 'Aderezos', icon: <Droplet size={30}/>, color: 'bg-yellow-100 text-yellow-700' }, { label: 'Bebidas', icon: <Wine size={30}/>, color: 'bg-purple-100 text-purple-700' }];
    return (<div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-900/60 p-4"><div className="bg-white rounded-3xl w-full max-w-4xl h-[80vh] flex flex-col"><div className="px-8 py-6 border-b flex justify-between"><h2 className="text-2xl font-bold">Uso Manual / Mermas</h2><button onClick={onClose}><X/></button></div><div className="p-8 overflow-y-auto"><div className="grid grid-cols-4 gap-6">{categories.map((c,i)=><button key={i} className={`p-6 border-2 rounded-2xl flex flex-col items-center ${c.color}`}>{c.icon}<span className="font-bold uppercase mt-2">{c.label}</span></button>)}</div></div></div></div>);
}

// -------------------------------------------------------------------------
// 7. MODAL BURGERS (COMPLETO - SPLIT VIEW)
// -------------------------------------------------------------------------
function BurgerProductionModal({ onClose }: { onClose: () => void }) {
    const [cuts, setCuts] = useState([{id:1,name:'Roast Beef',weight:''}]); const [fatT, setFatT] = useState(33); const [pattieW, setPattieW] = useState(120); const [fatChecked, setFatChecked] = useState(false);
    const meatSum = cuts.reduce((a,c)=>a+(parseFloat(c.weight)||0),0); const totalMix = meatSum > 0 ? meatSum / (1 - (fatT/100)) : 0; const fatNeed = totalMix - meatSum; const pattiesCount = Math.floor((totalMix * 1000) / pattieW);
    const updateCut = (id: number, field: string, value: string) => { setCuts(cuts.map(c => c.id === id ? { ...c, [field]: value } : c)); };
    return (
        <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-900/80 p-4 animate-in fade-in zoom-in-95 duration-200">
            <div className="bg-white rounded-2xl w-full max-w-6xl h-[90vh] flex flex-col shadow-2xl overflow-hidden">
                <div className="px-8 py-5 border-b flex justify-between items-center bg-white relative"><div><h2 className="text-xl font-bold flex gap-2 text-blue-600 items-center"><Scale/> Producción de Burgers</h2><p className="text-xs text-slate-400">Calculadora de Blend y Rendimiento</p></div><button onClick={onClose} className="p-2 hover:bg-slate-100 rounded-full transition-colors"><X size={24} className="text-slate-400"/></button></div>
                <div className="flex flex-1 overflow-hidden"><div className="w-1/2 flex flex-col p-8 border-r border-slate-100 overflow-y-auto"><h3 className="font-bold mb-4 text-slate-700 text-sm uppercase tracking-wide">1. Selección de Cortes (Carne Limpia)</h3><div className="space-y-3 mb-4">{cuts.map((cut, index) => (<div key={cut.id} className="flex gap-2 items-center"><div className="w-8 h-8 rounded-full bg-slate-100 flex items-center justify-center text-xs font-bold text-slate-500 shrink-0">{index + 1}</div><input className="border border-slate-200 p-3 rounded-lg w-full bg-slate-50 focus:bg-white focus:ring-2 focus:ring-blue-500 outline-none transition-all font-medium text-sm" placeholder="Nombre del Corte" value={cut.name} onChange={e=>updateCut(cut.id, 'name', e.target.value)} /><div className="relative w-28 shrink-0"><input className="border border-slate-200 p-3 rounded-lg w-full text-right font-bold bg-slate-50 focus:bg-white focus:ring-2 focus:ring-blue-500 outline-none transition-all text-sm" type="number" placeholder="0.00" value={cut.weight} onChange={e=>updateCut(cut.id, 'weight', e.target.value)} /><span className="absolute right-3 top-3 text-xs font-bold text-slate-400">KG</span></div>{cuts.length > 1 && (<button onClick={()=>setCuts(cuts.filter(c=>c.id!==cut.id))} className="p-3 text-slate-400 hover:text-red-500 hover:bg-red-50 rounded-lg transition-colors"><Trash2 size={16}/></button>)}</div>))}</div><button onClick={()=>setCuts([...cuts,{id:Date.now(),name:'',weight:''}])} className="text-blue-600 font-bold text-sm flex items-center gap-2 hover:bg-blue-50 px-3 py-2 rounded-lg transition-colors w-max mb-8"><Plus size={16}/> Agregar otro corte</button><div className="border-t border-slate-100 pt-8"><h3 className="font-bold mb-4 text-slate-700 text-sm uppercase tracking-wide">2. Configuración de Blend</h3><div className="flex gap-6"><div className="flex-1"><label className="font-bold text-xs block mb-2 text-slate-500">Grasa Deseada (%)</label><div className="relative"><input type="number" value={fatT} onChange={e=>setFatT(Number(e.target.value))} className="border border-slate-200 p-3 rounded-lg w-full font-bold text-center text-lg outline-none focus:border-blue-500"/><span className="absolute right-4 top-4 font-bold text-slate-400">%</span></div></div><div className="flex-1"><label className="font-bold text-xs block mb-2 text-slate-500">Peso Medallón (gr)</label><div className="relative"><input type="number" value={pattieW} onChange={e=>setPattieW(Number(e.target.value))} className="border border-slate-200 p-3 rounded-lg w-full font-bold text-center text-lg outline-none focus:border-blue-500"/><span className="absolute right-4 top-4 font-bold text-slate-400">gr</span></div></div></div></div></div><div className="w-1/2 flex flex-col bg-slate-50/50 p-8"><h3 className="font-bold text-slate-700 mb-6 uppercase text-xs tracking-wider flex items-center gap-2"><Calculator size={16}/> Resumen de Producción</h3><div className="flex justify-between p-4 bg-white rounded-xl shadow-sm border border-slate-100 mb-4 items-center"><span className="text-slate-500 font-bold text-sm">Carne Limpia Total</span><span className="text-xl font-bold text-slate-800">{meatSum.toFixed(2)} <span className="text-sm text-slate-400">kg</span></span></div><div className="bg-amber-50 p-5 rounded-xl border border-amber-100 mb-4 flex items-center justify-between relative overflow-hidden"><div className="relative z-10"><span className="block text-amber-800 font-bold uppercase text-xs mb-1">Grasa a Agregar</span><span className="text-xs text-amber-600">Para llegar al {fatT}% de blend</span></div><div className="relative z-10"><span className="text-3xl font-black text-amber-600">{fatNeed.toFixed(2)} <span className="text-lg">kg</span></span></div><Droplet className="absolute -right-2 -bottom-4 text-amber-500/10 w-24 h-24 rotate-12" /></div><div className="grid grid-cols-2 gap-4 mb-8"><div className="p-4 bg-blue-50/50 rounded-xl border border-blue-100"><span className="text-blue-600 font-bold text-[10px] uppercase block mb-1">Peso Total Blend</span><span className="text-2xl font-bold text-blue-700">{totalMix.toFixed(2)} <span className="text-sm">kg</span></span></div><div className="p-4 bg-green-50/50 rounded-xl border border-green-100"><span className="text-green-600 font-bold text-[10px] uppercase block mb-1">Medallones ({pattieW}g)</span><span className="text-2xl font-bold text-green-700">{pattiesCount} <span className="text-sm">u.</span></span></div></div><div className="mt-auto pt-6 border-t border-slate-200"><label className={`flex items-center gap-4 p-4 rounded-xl border transition-all mb-4 cursor-pointer ${fatChecked ? 'bg-white border-green-500 shadow-sm' : 'bg-white border-slate-200'}`}><div className={`w-6 h-6 rounded-full border-2 flex items-center justify-center transition-colors ${fatChecked ? 'bg-green-500 border-green-500' : 'border-slate-300'}`}>{fatChecked && <CheckCircle2 size={16} className="text-white"/>}</div><div><span className="font-bold text-sm block text-slate-700">CONFIRMAR AGREGADO DE GRASA</span><span className="text-xs text-slate-400">He pesado y agregado los {fatNeed.toFixed(2)} kg de grasa.</span></div><input type="checkbox" className="hidden" checked={fatChecked} onChange={e=>setFatChecked(e.target.checked)}/></label><button onClick={()=>{alert(`Producción Finalizada: ${pattiesCount} medallones ingresados.`); onClose()}} className={`w-full py-4 rounded-xl font-bold text-sm uppercase tracking-wide shadow-lg transition-all ${fatChecked && meatSum > 0 ? 'bg-slate-900 text-white hover:bg-slate-800 active:scale-95' : 'bg-slate-200 text-slate-400 cursor-not-allowed'}`} disabled={!fatChecked || meatSum <= 0}>Finalizar Producción</button></div></div></div></div>
        </div>
    );
}

// -------------------------------------------------------------------------
// 8. MODAL CARNICERÍA (COMPLETO - 3 COLUMNAS)
// -------------------------------------------------------------------------
function ButcheryModal({ onClose }: { onClose: () => void }) {
    const [sourceProduct, setSourceProduct] = useState("");
    const [inputWeight, setInputWeight] = useState<string>("");
    const [mode, setMode] = useState<'unit' | 'weight'>('unit');
    const [outputProduct, setOutputProduct] = useState("");
    const [unitWeight, setUnitWeight] = useState<string>("150");
    const [producedQty, setProducedQty] = useState<string>("");
    const [fatWeight, setFatWeight] = useState<string>("");
    const [wasteWeight, setWasteWeight] = useState<string>("");
    const inW = parseFloat(inputWeight) || 0; const fW = parseFloat(fatWeight) || 0; const wW = parseFloat(wasteWeight) || 0;
    let producedWeight = 0; if (mode === 'unit') { producedWeight = ((parseFloat(producedQty) || 0) * (parseFloat(unitWeight) || 0)) / 1000; } else { producedWeight = parseFloat(producedQty) || 0; }
    const totalOutput = producedWeight + fW + wW; const difference = inW - totalOutput; const currentStock = 54.20;

    return (
        <div className="fixed inset-0 z-50 flex items-center justify-center bg-slate-900/80 p-4 animate-in fade-in zoom-in-95 duration-200">
            <div className="bg-white rounded-2xl w-full max-w-6xl h-[90vh] flex flex-col shadow-2xl">
                <div className="px-8 py-5 border-b flex justify-between items-center bg-white rounded-t-2xl"><div><h2 className="text-xl font-bold flex gap-2 text-rose-600 items-center"><Beef/> Carnicería: Desposte</h2></div><button onClick={onClose} className="p-2 hover:bg-slate-100 rounded-full transition-colors"><X size={24} className="text-slate-400"/></button></div>
                <div className="flex flex-1 p-8 gap-6 bg-slate-50 overflow-hidden">
                    <div className="w-1/3 flex flex-col"><div className="bg-white p-6 rounded-2xl shadow-sm border border-slate-100 flex-1 flex flex-col mb-4"><span className="bg-rose-100 text-rose-700 px-3 py-1 rounded-full text-xs font-bold w-max border border-rose-200 mb-4 uppercase tracking-wider">Paso 1: Origen</span><h3 className="text-lg font-bold text-slate-800 mb-1">¿Qué vas a procesar?</h3><p className="text-xs text-slate-400 mb-6">Se descontará del stock de Carnicería.</p><div className="space-y-6"><div><label className="text-xs font-bold text-slate-400 uppercase ml-1 mb-1 block">Producto Entero</label><input type="text" placeholder="Ej: Lomo con Cordón" value={sourceProduct} onChange={e=>setSourceProduct(e.target.value)} className="w-full p-4 border border-slate-200 rounded-xl bg-slate-50 focus:bg-white outline-none font-medium transition-all"/></div><div><label className="text-xs font-bold text-slate-400 uppercase ml-1 mb-1 block">Peso Bruto</label><div className="relative"><input type="number" placeholder="0.00" value={inputWeight} onChange={e=>setInputWeight(e.target.value)} className="w-full p-4 border border-slate-200 rounded-xl text-3xl font-bold bg-slate-50 focus:bg-white outline-none transition-all"/><span className="absolute right-4 top-5 text-slate-400 font-bold">KG</span></div></div></div></div><div className="bg-white p-5 rounded-2xl shadow-sm border border-slate-200"><h4 className="font-bold text-slate-700 text-sm mb-3 border-b border-slate-100 pb-2">Resumen</h4><div className="flex justify-between items-center text-sm mb-2"><span className="text-slate-500">Stock actual:</span><span className="font-mono font-bold text-slate-700">{currentStock.toFixed(2)} kg</span></div><div className="flex justify-between items-center text-sm"><span className="text-slate-500">Stock final estimado:</span><span className="font-mono font-bold text-rose-600">{(currentStock - inW).toFixed(2)} kg</span></div></div></div>
                    <div className="w-1/3 flex flex-col"><div className="bg-white p-6 rounded-2xl shadow-sm border border-slate-100 flex flex-col flex-1"><span className="bg-blue-100 text-blue-700 px-3 py-1 rounded-full text-xs font-bold w-max border border-blue-200 mb-4 uppercase tracking-wider">Paso 2: Producto</span><h3 className="text-lg font-bold text-slate-800 mb-1">¿Qué obtuviste?</h3><p className="text-xs text-slate-400 mb-6">Esto se dará de alta como nuevo stock.</p><div className="flex bg-slate-100 p-1 rounded-lg mb-6"><button onClick={()=>setMode('unit')} className={`flex-1 py-2 text-xs font-bold rounded ${mode==='unit'?'bg-white shadow text-blue-600':'text-slate-400'}`}>POR UNIDADES</button><button onClick={()=>setMode('weight')} className={`flex-1 py-2 text-xs font-bold rounded ${mode==='weight'?'bg-white shadow text-blue-600':'text-slate-400'}`}>POR PESO</button></div><div className="space-y-6 flex-1"><div><label className="text-xs font-bold text-slate-400 uppercase ml-1 mb-1 block">Nombre Producto Final</label><input type="text" value={outputProduct} onChange={e=>setOutputProduct(e.target.value)} placeholder={mode==='unit'?"Ej: Bifes de Lomo":"Ej: Carne Milanesa"} className="w-full p-4 border border-slate-200 rounded-xl bg-slate-50 focus:bg-white outline-none font-medium transition-all"/></div>{mode === 'unit' ? (<div className="grid grid-cols-2 gap-4"><div><label className="text-xs font-bold text-slate-400 uppercase ml-1 mb-1 block">Peso Unit (gr)</label><input type="number" value={unitWeight} onChange={e=>setUnitWeight(e.target.value)} className="w-full p-4 border border-slate-200 rounded-xl text-center font-bold bg-slate-50 outline-none"/></div><div><label className="text-xs font-bold text-slate-400 uppercase ml-1 mb-1 block">Cantidad</label><input type="number" value={producedQty} onChange={e=>setProducedQty(e.target.value)} placeholder="0" className="w-full p-4 border-2 border-blue-100 rounded-xl text-center text-xl font-bold text-blue-600 bg-white outline-none"/></div></div>) : (<div><label className="text-xs font-bold text-slate-400 uppercase ml-1 mb-1 block">Peso Limpio</label><div className="relative"><input type="number" value={producedQty} onChange={e=>setProducedQty(e.target.value)} placeholder="0.00" className="w-full p-4 border-2 border-blue-100 rounded-xl text-3xl font-bold bg-white outline-none transition-all text-blue-600"/><span className="absolute right-4 top-5 text-blue-300 font-bold">KG</span></div></div>)}</div></div></div>
                    <div className="w-1/3 flex flex-col"><div className="bg-white p-6 rounded-2xl shadow-sm border border-slate-100 flex flex-col flex-1"><span className="bg-amber-100 text-amber-700 px-3 py-1 rounded-full text-xs font-bold w-max border border-amber-200 mb-4 uppercase tracking-wider">Paso 3: Subproductos</span><h3 className="text-lg font-bold text-slate-800 mb-1">Grasa y Desperdicio</h3><p className="text-xs text-slate-400 mb-6">Crucial para el costo.</p><div className="flex-1 space-y-6"><div className="bg-yellow-50 p-4 rounded-xl border border-yellow-200"><label className="text-xs font-bold text-amber-700 uppercase ml-1 mb-2 block flex items-center gap-2"><Droplet size={14} className="fill-amber-500"/> Grasa (Para Burgers)</label><div className="relative"><input type="number" placeholder="0.00" value={fatWeight} onChange={e=>setFatWeight(e.target.value)} className="w-full p-3 border-2 border-amber-300 rounded-lg text-2xl font-bold bg-white text-amber-800 outline-none"/><span className="absolute right-4 top-4 text-amber-700/50 font-bold text-sm">KG</span></div><p className="text-[10px] text-amber-700 mt-2 leading-tight font-medium">Se suma al stock de Grasa.</p></div><div><label className="text-xs font-bold text-slate-400 uppercase ml-1 mb-2 block flex items-center gap-2"><Trash2 size={14}/> Desperdicio</label><div className="relative"><input type="number" placeholder="0.00" value={wasteWeight} onChange={e=>setWasteWeight(e.target.value)} className="w-full p-3 border border-slate-200 rounded-lg text-xl font-bold bg-slate-100 text-slate-500 focus:bg-white outline-none"/><span className="absolute right-4 top-4 text-slate-400 font-bold text-sm">KG</span></div></div><div className="mt-6 pt-6 border-t border-slate-100"><div className="flex justify-between text-xs font-bold uppercase mb-2"><span className="text-slate-700">Balance</span><span className={Math.abs(difference) < 0.05 ? 'text-green-600' : 'text-red-500'}>{Math.abs(difference) < 0.05 ? 'OK' : `Dif: ${difference.toFixed(2)}`}</span></div><div className="w-full h-3 bg-slate-100 rounded-full flex overflow-hidden border border-slate-200"><div style={{width: `${inW > 0 ? (producedWeight/inW)*100 : 0}%`}} className="bg-blue-500 h-full"></div><div style={{width: `${inW > 0 ? (fW/inW)*100 : 0}%`}} className="bg-amber-400 h-full"></div><div style={{width: `${inW > 0 ? (wW/inW)*100 : 0}%`}} className="bg-slate-400 h-full"></div></div></div></div><button onClick={()=>{alert("Transformación guardada");onClose()}} className="mt-auto w-full py-4 bg-slate-900 text-white rounded-xl font-bold hover:bg-slate-800 transition-all shadow-lg active:scale-95">CONFIRMAR</button></div></div>
                </div>
            </div>
        </div>
    );
}

// COMPONENTES UI
const NavItem = ({ icon, label, active }: any) => (<button className={`w-full flex items-center gap-3 px-3 py-3 rounded-xl text-sm font-medium transition-all ${active ? 'bg-blue-50 text-blue-700 shadow-sm' : 'text-slate-500 hover:bg-slate-50 hover:text-slate-900'}`}>{icon} <span>{label}</span> {active && <ChevronRight size={14} className="ml-auto text-blue-400" />}</button>);
const StationCard = ({ title, subtitle, icon, color, steps, action, lightIcon, onClick }: any) => (<div className="group bg-white border border-slate-100 rounded-2xl p-6 relative overflow-hidden transition-all duration-300 hover:shadow-lg hover:border-blue-100 hover:-translate-y-1"><div className="flex justify-between items-start mb-6"><div className={`w-14 h-14 rounded-2xl flex items-center justify-center shadow-sm ${color} ${lightIcon ? '' : 'shadow-blue-500/20'}`}>{icon}</div></div><h3 className="text-xl font-bold text-slate-800 mb-1">{title}</h3><p className="text-sm text-slate-400 font-medium mb-4">{subtitle}</p><button onClick={onClick} className="w-full py-3 bg-slate-900 hover:bg-blue-600 text-white font-bold rounded-xl transition-all shadow-lg active:scale-95 flex items-center justify-center gap-2">{action} <ChevronRight size={16} /></button></div>);
const QuickActionCard = ({ title, subtitle, icon, color, onClick }: any) => (<div onClick={onClick} className={`border-2 ${color === 'blue' ? 'border-blue-100 bg-blue-50/50 hover:bg-blue-50' : 'border-dashed border-slate-300 bg-white hover:border-slate-400'} rounded-xl p-6 transition-all cursor-pointer group flex flex-col items-center text-center`}><div className={`p-4 rounded-full mb-4 group-hover:scale-110 transition-transform ${color === 'blue' ? 'bg-blue-600 text-white' : 'bg-orange-50 text-orange-500'}`}>{icon}</div><h3 className="text-xl font-bold text-slate-800">{title}</h3><p className="text-sm text-slate-500">{subtitle}</p></div>);